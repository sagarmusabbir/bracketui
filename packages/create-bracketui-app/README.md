# create-bracketui-app

create-bracketui-app is a CLI tool to scaffold new projects using the Bracket UI component library.

## Installation

You can install the CLI globally via npm:

```bash
npm install -g @thirdbracket/create-bracketui-app
```
